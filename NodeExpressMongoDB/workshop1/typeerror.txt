const { DNS } = require("@google-cloud/dns");
const dns = new DNS();
const ZONE = dns.zone("INSERTZONENAMEHERE");

const aRecord = {
    zone_: "INSERTZONENAMEHERE",
    type: "A",
    metadata: {
      name: "testing",
      data: ["0 1 587 mail.example.com."],
      ttl: 300,
      type: "A",
    },
  };

if (!existingARecord) {
    await ZONE.addRecords("a", aRecord)
      .then((res) => {
        console.log(res);
      })
      .catch((err) => {
        console.log(err);
      });
  }